# 长截图模块安装说明

将压缩包内的 `Modules\LongCapture` 文件夹合并到轻截程序旁的 `Modules`
文件夹，然后等待轻截自动识别，或在托盘菜单中选择“重新加载模块”。

安装后的入口文件为：

```text
Modules\LongCapture\ScreenshotTool.LongCapture.dll
```

你可以在设置工作台的“插件模块”页面启用、禁用或永久删除它。永久删除后，
需要重新从轻截发布页面下载此模块才能恢复。
